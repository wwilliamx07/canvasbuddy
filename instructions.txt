when no saved chat is selected, but the user starts chatting, it should save the current chat as a new chat. add support for md text formatting for ai responses in the chat ui. when the model makes a tool call, strip the tool call itself from the message that is displayed to the user. refactor the model's base url, gpt model, and api key to be able to be set in the settings page. these settings should persist, and the api key should be starred out. they should also default to the current hardcoded settings.