# 07 — UI

Sources: `src/App.tsx` (state owner), `src/components/{Navigation,ChatUI,Settings,GraphExplorer}`, `src/index.css`, `src/App.css`.

## Component tree and state ownership

```
<App>                         owns: activeTab, chats[], currentChatId, messages, contextDigests,
 │                                  isLoading, settings, currentContextTokens
 ├── <Navigation>             left rail (w-48): Chat / Memory / Settings tabs (tab id 'graph'); chat list with
 │                            new/select/delete (only rendered on the Chat tab)
 └── <main>
      ├── <Connect>           shown instead of Chat/Memory until a Canvas host is connected
      ├── <ChatUI>            messages + input; props only, no state beyond the input box
      ├── <GraphExplorer>     self-contained: loads its own data from db/* and canvas/sync
      └── <Settings>          controlled form; auto-saves every change via onSettingsChange
```

`App` also owns the `connection` state (`checking` → `connected` | `disconnected`): after settings load it checks the remembered `settings.canvasHost` against `chrome.permissions`, calls `activateCanvas(host)` (which sets the host in `canvas/http.ts` and picks the profile), resolves the identity (`resolveIdentity`) and its memory slot (`memorySlotFor`), opens that database (`configureDatabase`) and loads that slot's chats, and derives the session's system prompt from the profile. Chat and Memory render only when connected; Settings always renders. A `notice` banner above the main area reports a signed-out session ("showing what's remembered for <name>") or another account signing in ("Reload to switch memory"), with a Reload button.

`App` is the single owner of chat and settings state and is the only place that writes `localStorage`. `GraphExplorer` is the exception to "props down": it reads and writes the database directly because it is a debugging/administration surface over the same data layer the agent uses.

Tabs are mutually exclusive (`activeTab === 'x' && …`), so switching away from the Memory tab unmounts the explorer and its in-memory state (selected course, collapsed modules); switching back reloads from PGlite.

## ChatUI

- Renders user bubbles as plain text and assistant bubbles as Markdown through `renderMarkdown` (`utils/markdown.ts`: `marked` → DOMPurify with an explicit tag/attribute allowlist; links get `target="_blank"` so they don't navigate the panel). Assistant text is derived from Canvas content that other people author, so raw HTML is never injected unsanitized — the CSP blocks scripts but not `<style>`/`<form>`/`<iframe>` overlays.
- Markdown also renders LaTeX: `$…$` and `\(…\)` inline, `$$…$$` (own line, or mid-paragraph) and `\[…\]` display. A `marked` extension tokenizes the math and emits a `<span data-math="N">` placeholder instead of HTML; `renderMarkdown` sanitizes the placeholder-bearing output with DOMPurify first, then replaces each placeholder with `katex.renderToString(...)` — placeholder → sanitize → replace, in that order, so KaTeX's own markup never has to pass the Markdown allowlist (see `08-conventions-and-gotchas.md`).
- Auto-scrolls to the newest message. Input is disabled while `isLoading`; the Send button becomes a Stop button (`onStop` → `AbortController` in `App`).
- **Streaming.** Each model turn gets its own bubble, created empty with `streaming: true` before the call: typing dots until the first token, then Markdown re-rendered per animation frame with a blinking caret (`.streaming-caret` in `App.css`). The dots row under the transcript shows only when no bubble is streaming (tool execution, digesting).
- **Activity.** A turn that ends in tool calls keeps its text and gets `activity`: one short line per call ("Listing items \"lecture 4\"", "Searching the document for …" — `describeToolCall` in `App.tsx`), rendered as a small list under the text. It is persisted with the chat; `streaming` is not.
- Chats are titled from their first message.
- Styling: light theme (`bg-gray-50`), Tailwind `prose` classes for Markdown, word-break rules so long tokens don't overflow the narrow side panel.

## Connect

First-run screen (`components/Connect/Connect.tsx`): a host field prefilled from the active tab (`activeTabHost`, needs the `activeTab` grant an action click gives), a Connect button that — inside the click — requests the profile's origins, verifies the session (`verifyCanvasSession`), and hands the host up to `App`; errors (permission denied, not signed in, not a Canvas site) show inline. It is shown again whenever the origin permission is missing.

## Settings

`AppSettings` (exported from `Settings.tsx`, used everywhere):

```ts
{ apiKey, baseUrl, model, embeddingModel?, llmProvider: 'openai' | 'google', contextThreshold, freshness? }
```

- Every field auto-saves on change — there is no other mode. A caption under the title reads "Changes save automatically", and `handleChange` sets a transient `justSaved` state that shows a "Saved" pill next to the title for ~1.5 s (a `useRef` timeout so rapid keystrokes reset it instead of stacking).
- The API Key field shows a non-blocking hint when the key is non-empty and doesn't match the selected provider's format (Google keys start with `AIza`, OpenAI with `sk-`); it never blocks saving or clears the value, and stays quiet for OpenAI when a custom `baseUrl` is set (compatible providers use other key formats).
- `baseUrl` is optional. Empty means the provider default (`DEFAULT_BASE_URLS` in `src/settings.ts`, shown as the field's placeholder); every chat and embedding call goes through `resolveBaseUrl(settings)`, so a custom value points both at an OpenAI-compatible (or Gemini-compatible) endpoint. The default is resolved at call time, never stored, so switching providers does not carry the other provider's URL along.
- Shows the live context estimate against the threshold (see `02-agent-loop.md` for what the estimate excludes).
- Defaults live in `src/settings.ts` (`DEFAULT_SETTINGS`, `normalizeSettings`): provider `google`, model `gemini-3.5-flash-lite`, embedding `gemini-embedding-2`, threshold 15,000, `freshness` = `DEFAULT_FRESHNESS`. Existing users keep whatever is persisted — the default only applies on first install.
- **Canvas row** at the top: "Connected to <host> · Quercus / Memory for <name>" with **Forget this memory** (`onForgetMemory` → confirm → `closeDB` + `forgetMemory` + reload) and **Disconnect** (`onDisconnect` → releases the origin permission and clears `canvasHost`, which brings the Connect screen back; memory is kept), or "Not connected" when the Connect screen is showing.
- **Freshness section**: one numeric (minutes) field per collection plus probe debounce and unavailable-retry, with a reset-to-defaults button. Writes `settings.freshness`; the engine reads it on every `ensureCollection` call, so changes apply immediately.

## Graph Explorer

In the UI this surface is called **Memory** (nav tab, heading, empty states); code identifiers and this reference keep "graph".

A dark-themed, side-panel-sized browser over the knowledge graph. The subtitle names whose memory it is (`memoryLabel`: "<name> · <host>").

Layout: header (title + "Sync Courses") → stats ribbon (six counts from `getGraphStatistics`) → course selector + module filter → course header with "Refresh Course" → module tree (collapsible, items listed under each) → "Pages" and "Files not in a module" sections → a right-hand detail panel for the selected node.

Data loading:
- On mount: statistics, courses (`exploreGraph({entity_type:'courses'})`), all tracked documents (`getFilesList()`); selects the first course.
- On course change: `exploreGraph({entity_type:'full_hierarchy'})` (with items) and `exploreGraph({entity_type:'pages', limit:200})`.
- On node selection: if the node maps to a document (`indexTargetFor`), loads its chunks with `getFileChunks`.

Actions:
- **Sync Courses** → `ensureCollection('courses', {}, { settings, refresh: true })` then reload.
- **Refresh Course** → `ensureCollections(['modules','assignments','files','pages','home'], { courseId }, { settings, refresh: true })` in series; the status pill summarizes which were updated, already current, unavailable in this course, or failed. Then reload the tree.
- **Index for search** (detail panel, when the node is a file/page/assignment) → `indexDocumentJustInTime(target, settings)` then refresh files/stats/chunks.

`indexTargetFor(node, courseId)` is the small adapter that turns any tree/list node (module item, page row, assignment row, loose file row) into the `{docId, IndexTarget}` pair the indexing pipeline needs; it builds doc ids with `docIdFor()` from `db/rag.ts`, the same function the tools and indexing use.

Status messages (`syncStatusMsg`) are transient (4 s) and shared by all actions; `isSyncing` disables both sync buttons while any sync runs.

## Navigation

Tabs plus the chat list. Chat rows show title and last-updated date; delete is revealed on hover. "New Chat" creates an empty chat immediately (it is also created implicitly if the user sends a message with no active chat).

## Styling notes

- Tailwind v4 via `@import "tailwindcss"` in `index.css`; typography plugin for Markdown.
- The side panel is narrow (often ~400 px). Components use `min-w-0`, `truncate`, and responsive `sm:`/`md:` breakpoints to stay usable; the Graph Explorer hides its subtitle and badge below `md`.
- `App.css` carries a `animate-fade-in` keyframe used by chat bubbles and status pills.
